// Shared copy for the three inline-edit field editors (InlineTextField, InlineSelectField,
// InlineDateField) so the failed-save message is defined once instead of duplicated per file.
export const SAVE_ERROR_MESSAGE = "Couldn't save";
