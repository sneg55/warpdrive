// Named event constant for opening the command palette via window CustomEvent.
// Using a constant avoids magic strings in both the trigger and the listener.
export const OPEN_SEARCH_EVENT = "warpdrive:open-search" as const;
