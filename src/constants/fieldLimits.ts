// Deal and lead titles are capped by dealCreateInput/leadCreateInput (Zod .max(255)); this
// constant drives the live counter shown under the title input so it matches server validation.
export const TITLE_MAX_LEN = 255;
