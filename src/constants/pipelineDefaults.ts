// Default stage settings used when adding a stage or toggling rotting on. Named so the same value
// is not duplicated across the stage editor and the pipeline editor.

// Days a deal may sit in a stage before it is flagged as rotting, applied when rotting is enabled.
export const DEFAULT_ROTTING_DAYS = 7;
