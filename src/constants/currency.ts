// Fallback currency used when the tenant base currency (settings.base_currency) has not loaded or
// is unset. Kept in one place so the client fallbacks cannot drift from each other.
export const DEFAULT_BASE_CURRENCY = "USD";
